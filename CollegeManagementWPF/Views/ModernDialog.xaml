<Window x:Class="CollegeManagementWPF.Views.ModernDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="NoResize"
        SizeToContent="WidthAndHeight" WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Window.Resources>
        <Style x:Key="OkBtn" TargetType="Button">
            <Setter Property="Height" Value="38"/><Setter Property="MinWidth" Value="100"/>
            <Setter Property="FontSize" Value="13"/><Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="Segoe UI"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/><Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" CornerRadius="8" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="bd" Property="Opacity" Value="0.82"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Margin="12" CornerRadius="14">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="4" Opacity="0.55" Color="#000000" Direction="270"/>
        </Border.Effect>
        <Border CornerRadius="14" ClipToBounds="True" MinWidth="380" MaxWidth="480">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#111827" Offset="0"/><GradientStop Color="#0D1421" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <StackPanel>
                <Border x:Name="AccentBar" Height="4" CornerRadius="14,14,0,0"/>
                <StackPanel Margin="28,22,28,24">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                        <Border x:Name="IconBadge" Width="40" Height="40" CornerRadius="10" Margin="0,0,14,0" VerticalAlignment="Center">
                            <TextBlock x:Name="IconText" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock x:Name="TitleText" FontSize="16" FontWeight="Bold" Foreground="White" FontFamily="Segoe UI" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Rectangle Height="1" Margin="0,0,0,16">
                        <Rectangle.Fill>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="Transparent" Offset="0"/>
                                <GradientStop Color="#1E3A6B" Offset="0.5"/>
                                <GradientStop Color="Transparent" Offset="1"/>
                            </LinearGradientBrush>
                        </Rectangle.Fill>
                    </Rectangle>
                    <TextBlock x:Name="MessageText" FontSize="13" Foreground="#B0C4DE" FontFamily="Segoe UI" TextWrapping="Wrap" LineHeight="20" Margin="0,0,0,22"/>
                    <Button x:Name="OkButton" Content="OK" Style="{StaticResource OkBtn}" HorizontalAlignment="Center" Click="OkButton_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Border>
</Window>
